#Restituisce la densita'
dexp(d, rate)

#Restituisce la funzione distribuzione
pexp(p, rate)

#Restituisce la funzione quantile
qexp(q, rate)

#Genera deviazioni casuali
rexp(r, rate)

#Se rate non e' specificato, assume di default valore 1