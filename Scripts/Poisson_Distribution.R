#Variabile aleatoria di Poisson
#Restituisce la densita'
dpois(d, lambda)

#Restituisce la funzione distribuzione
ppois(p, lambda)

#restituisce la funzione quantile
qpois(q, lambda)

#Restituisce deviazioni casuali
rpois(r, lambda)

#Quando va utilizzata e' sempre 
#specificato nell'esercizio
